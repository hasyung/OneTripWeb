<div id="audios" class="well newest-audios">
	<h6>最新添加的景区音频</h6>
	<% if can? :read, Audio %>
		<ul class="nav nav-list">
			<% @newest_audios.each do |audio| -%>
				<li>
					<a href="<%= "#{admin_place_path(audio.place_id)}#audio" %>">
						<i class="icon-volume-up"></i><%= " #{audio.place.name} - #{audio.name.truncate(26)}" %>
					</a>
				</li>
			<% end -%>
		</ul>
	<% else -%>
		<p><%= t("unauthorized.index.audio") %></p>
	<% end -%>
</div>